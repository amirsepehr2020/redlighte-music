{
  "name": "redlighte-music",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute prod-d1-tutorial --file=schema.sql --remote",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250430.0",
    "typescript": "^5.5.2",
    "vitest": "~3.0.7",
    "wrangler": "^4.14.0"
  }
}
